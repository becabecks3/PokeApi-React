import React from "react";

const Card = ({search}) => {
  return(
    <>
    <section>
      <h2>{data.results.name}</h2>
      <img src={data.sprites.front_default} />
    </section>
    </>
  )
};

export default Card;
